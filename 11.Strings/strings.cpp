#include <iostream>
#include <string>
using namespace std;

int main () {
    // string name;
    // int age;

    // cin >> age;

    // getline(cin, name);

    // cout << name << " " << age;

    //

    // string a = "rishabh";
    // cout<<a[6];
    // return 0;

    // string a, b;
    // cin >> a >> b;

    /*
    // >
    if (a > b) {
        cout << "a is greater than b ";
    } else if (a < b) {
        cout << "a is less than b ";
    }


    // <

    // ==
    if (a == b) {
        cout << "a is equal to b";
    } else {
        cout << "a is not equal to b";
    }
*/

    // string a = "sishabh";
    // string b = "rishabk";
    // int ans = (a>=b);
    // cout<<ans;

    // return 0;

     string a = "rishabh";
    a.pop_back();
    a.erase(4);
    cout<<a;

}
